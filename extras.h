#ifndef _EXTRAS_H_
#define _EXTRAS_H_

#include "record.h"

// function prototypes
void defragment(Record[]);
void display_stats(Record[]);
void sort_record(Record[]);
void swap(Record* i, Record* j);

#endif
